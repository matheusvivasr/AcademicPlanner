export const enum NamedRanges {
    /** The search field in the dashboard. */
    SearchTarget = 'searchedField',
    /** The selected search type in the dashboard. */
    SearchType = 'searchType',
    /** The searched/targeted subject's ID.*/
    TargetID = 'targeted_ID',
    /** ID from all saved subjects. */
    AllSavedIDs = 'allSaved_IDs',
  }